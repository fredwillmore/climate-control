<turbo-frame id="playlist_item">
  <%= turbo_frame_tag 'track', src: (@playlist_item ? track_path(@playlist_item.track) : tracks_path) %>
  <%= turbo_frame_tag 'artist', src: (@playlist_item ? artist_path(@playlist_item.track.artist) : artists_path) %>
</turbo-frame>
